# Bundled Markdown-template locales

Auto-generated from `BundledLanguageTemplates.availableLocales()` (`SkillFactsExporter`). Do **not** edit by hand — run `./gradlew syncSkillFacts`.

Selected with `markdown.template.language=<tag>`.

| | |
|---|---|
| Selectable locales | 22 |
| Bundled templates | 21 |
| Templates directory | `src/main/resources/markdown/templates/` |

Bundled locale tags: `ar`, `de`, `es`, `fr`, `hi`, `id`, `it`, `ja`, `ko`, `nl`, `pl`, `pt`, `pt-BR`, `ru`, `th`, `tr`, `uk`, `vi`, `zh`, `zh-CN`, `zh-TW`.

`en` is selectable but **not** in the registry — it uses the built-in default template. Locale fallback (exact → language subtag → first `lang-*` sibling) is implemented by `BundledLanguageTemplates` and documented in `rule-guide.md`.

